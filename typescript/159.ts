// premium problem
//Sliding window

function lengthOfLongestSubStringTwoDistinct(s: string): string {
      //base
      let arrayChar :string = [...s];

      //define pointer
      let left :number = 0;
      let right :number = 0;
  
      //define flag
      let sum :number = 0 ;
      let maxLength :number = Number.MIN_SAFE_INTEGER;

      

      
};
